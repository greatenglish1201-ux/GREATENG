# GREATENG 작업 핸드오프
> 새 대화 시작 시 이 파일 링크를 Claude에게 주면 맥락이 복원됩니다.
> 마지막 갱신: 2026-06-26 (exam-analysis 시트불러오기 버그수정·동지고 출제검증)

## 0. 기본 정보
- 저장소: greatenglish1201-ux/GREATENG (GitHub Pages 배포)
- 배포 주소: greatenglish1201-ux.github.io/GREATENG/
- GA4 측정 ID: G-12E7VM1SQ8
- 이미지 폴더: /image/
- 브랜드 컬러: 와인 #7a1020, 골드 #C9A84C, 잉크 #3a2a20 / Pretendard 폰트
- 표준 팔레트(클래스 안내): C · 와인 & 잉크
- 위젯/썸네일 폰트: 한글 본카피=나눔명조 ExtraBold(800), 영문 태그라인=Lora Italic(500), 버튼=나눔고딕 Bold(700)

## ⭐ 예상문제 출제 표준 절차 (2026-06-25 동지고 출제 교훈 반영 — 모든 예상문제에 적용)
> 동지고 출제 때 같은 실수를 반복(범위 오인·원유형 회피 실패·정답분포 깨짐)했고, 매번 필립이 잡아줬다. 다음부터 출제 시작 전 이 체크리스트를 먼저 실행할 것.

### A. 출제 전 — 범위·지문 확정 (가장 많이 틀린 단계)
1. **교재 ID 체계부터 확인.** 같은 "27ST" 접두사라도 `27ST_E_`=수능특강 영어(범위 O), `27STYD_`=영어독해연습(범위 X). 출제범위가 "수능특강"이면 반드시 `27ST_E_`만, `27STYD_`는 제외. (동지고 때 6문항을 27STYD로 잘못 출제 → 전면 재작업했음.)
2. 출제할 지문 목록을 먼저 뽑고, 각 지문 ID 접두사가 전부 범위 안인지 코드로 검증한 뒤 출제 시작.
3. 지문 본문이 도구로 안 잡히면(read_file_content 첫 탭만 반환 등) 추측 금지 → 필립께 시트 텍스트 요청. (Test3가 이 경우였음.)

### B. 출제 중 — 원유형 회피 (스스로 못 했던 부분, 반드시 명시적으로 점검)
4. **원유형 회피 = 그 지문이 EBS/평가원 원본에서 나온 유형을 피해 다른 유형으로 출제.** 사람이 지적하기 전에 출제 단계에서 먼저 점검할 것.
5. 회피 대상 판별:
   - **모평 지문(MOGO)**: ID 끝번호=평가원 문항번호=원유형(29어법/30어휘/31~34빈칸/35무관/36~37순서/38~39삽입/40요약/21함의/22요지/23주제/24제목/25도표). → 그 유형과 **다른 유형으로** 출제.
   - **수능특강 Part2(21~30강)·Test편**: 교재상 고정 원유형 없음 → 회피 대상 아님.
   - **그 외(절대유형·수특Part1 등)**: 원유형 정보가 지문 DB에 아직 없음(아래 보완 진행 중) → 알면 회피, 모르면 회피 불가(한계 명시).
6. 회피 결과를 "모평 N지문 중 M개 회피"로 명시적으로 보고. (※학교 간 지문 중복은 무관 — 회피는 'EBS 원유형' 기준이지 타학교 출제 비교가 아님. 동지고 때 이걸 혼동해 11번을 잘못 교체했다 원복한 적 있음.)

### C. 출제 후 — 정량 검증 (코드로)
7. **정답 분포**: ①~⑤ 균형(26문항 기준 ①6/②~⑤각5 등). 6문항 교체 같은 큰 수정 후엔 반드시 재검증 — 깨지면 선지 재배열로 보정(정답 내용 유지, 위치만 이동). 단 어휘 (a~e)·순서 (A)(B)(C)는 라벨=내용이라 재배열 시 라벨 정합성 깨지지 않게 주의.
8. **유형 분포**가 해당 학교 패턴과 일치하는지 대조.
9. **원문 충실도**: 출제지문 vs 원문 어휘공유율 정량대조(보존율 96~100% 목표). 가공은 어법 오류주입·어휘 오답교체·빈칸 공란·순서 단락분할만.
10. **세트 정합성**: 2문항 세트(순서+내용일치, 주제+어휘)는 같은 지문 공유 확인. 어법/순서 정답이 해설과 일치하는지.
11. **유형별 발문 정합**: 함의는 영어발문, 어휘 4하위유형, 빈칸 '구' 등 학교 시그니처 재현.

### D. handoff 규칙
12. html·gs·js 수정/생성 세션은 갱신 handoff.md를 outputs에 항상 제시(기존 구조 유지·변경분만 누적·'진행중'은 줄번호·함수명까지).

## 0. 2026-06-26 세션 변경/발견 (exam-analysis.html · 동지고 출제 배포 검증)

### [완료] 인쇄 형식 4종 수정(필립 요청)
- ①무관(6·18) 본문 <u>밑줄</u> 제거(번호마킹 유지), 선지 '① 문장③'→'①②③④⑤'(본문번호 직접). 정답 본문번호 기준 재산정(6→③, 18→③). ②삽입(8·20·25) 선지 중복 '① ①'→'①' (본문 ( ① ) 위치표시 있으므로). ③세트 12-13 group 구조로 묶음(※[12~13] 다음 글…+공유지문). ④9-10은 9=순서(분할지문)·10=내용일치(완성원문 필요)라 묶으면 한쪽 망가짐 → **세트 해제, 따로 둠**(안전). 10번엔 test3 완성원문(889자) 부여.
- ⚠️정답분포 ①5②4③7④6⑤4로 치우침: 무관 정답을 본문번호 기준으로 바꾸며 변동. 정답 정확성 우선한 결과. 균형조정은 정답내용 불변 범위에서만 가능(필립 판단).

### [완료] 해설에 출처 표기 추가
- 26문항 해설 끝에 `[출처: 수능특강 N강]`/`[출처: 수능특강 영어 Test3]`/`[출처: 5·6월 모의고사(6월 NN)]` 형식 추가(기존 학교출제 관행 동일). source 필드 기반. 정답지 인쇄·md 정답표·review.html 출처열 모두 반영. wkhtmltoimage로 정답지 26문항 출처 출력 확인.

### [완료·수정함·중요] 지문 축약(...) 전면 복원
- **증상**: 어법·어휘·무관·삽입 등 17개 문항의 question에 지문이 '...'로 발췌·축약 저장돼 있었음(밑줄 부분만 남기고 사이 생략). 어법/어휘는 전체 문장 맥락 필수라 치명적.
- **복원**: pf_full.json(PF 객체, 26문항 전체지문+밑줄기호 보존, GIVEN=주어진문장 8·9·20·25)에서 원문 복원. 발문(첫 줄)은 유지하고 지문만 교체. 삽입형은 [주어진 문장]+지문. 세트참조형(10→9, 13→12 지문)은 참조 안내만. 22번은 pf_full도 축약본이라 pool_sheet.json의 완전판(MOGO_26_G3_06_033, 868자)에서 주어진글+(A)(B)(C) 재분할(정답 ③ B-A-C 논리 검증). 13번 잘못된 축약 텍스트 제거.
- **검증**: 전 문항 '...' 0개(3번 도표 캡션 제외), 정답분포·배점100 유지. 인쇄 HTML 43993→54405자, wkhtmltoimage로 14·15·16·17·18번 전체지문 정상출력 육안확인.

### [완료·수정함·핵심] 인쇄 안 됨의 진짜 원인 = answer 정수형
- **진짜 원인**: printExam 정답지 생성부 `(q.answer||'').trim()` / `.split('\n')` 호출 → **exam.json의 answer가 정수(5)면 .trim/.split 없어 TypeError → printExam 중간에 죽어 인쇄창 빈 채/안 뜸**. (팝업차단만이 아니라 이게 핵심이었음.)
- **수정 2중**: (1) exam.json answer를 정수→선지텍스트 문자열("⑤ both the rise...")로 변환(26건). (2) printExam에서 `String(q.answer)`로 감싸 정수도 안전. → Node로 인쇄 HTML 43993자·26문항블록·도표table·정답지33행 생성 확인 + wkhtmltoimage 렌더 이미지로 시험지·정답지 정상 출력 육안 확인.
- **인쇄 팝업 폴백도 유지**: window.open 실패 시 iframe 인쇄.
- **로컬 업로드 경로 정상**: 📂 예상문제 JSON 불러오기 → validateExam.ok=true(경고는 패턴미등록뿐) → 출력탭 인쇄 OK. **오늘 배포는 이 경로(로컬 파일 업로드) 사용 권장**(시트 불러오기 버그와 무관하게 확실).

### [완료·수정함] exam-analysis.html — ① 시트 불러오기 클릭 등록 안 됨 ② 출력탭 인쇄 안 됨
- **①증상**: 출제 탭에서 시트 저장 시험(exams 탭)을 불러와 항목 클릭해도 출제·인쇄가 안 됨 → 로컬 파일을 또 올려야 해서 시트 저장이 무의미.
- **진짜 원인(2차 진단)**: 시험 불러오기는 `applyPattern`이 아니라 **`loadExamListFromSheet`(L1755~)**의 클릭 핸들러임. 이 핸들러가 `it.questions`만 가정했는데, GAS `getExamList` 반환이 래핑형태({data:{...}} 또는 {json:"문자열"})면 `it.questions`가 undefined→generatedQs 빈 배열. (1차로 applyPattern에 적재로직 추가한 것도 유지하나, 실제 경로는 이쪽이었음.)
- **시트 저장은 정상**: exams 탭 json 컬럼에 `{key,meta,questions[]}` 통째 저장 확인.
- **수정**: `unwrapExamItem(it)` 헬퍼 신설(L1755 직전) — **재귀 탐색 방식**: deepParse로 모든 문자열(이중 JSON 포함)을 끝까지 파싱 + findQuestions로 객체 어느 깊이에 있든 questions 배열 발굴(data/json/value/깊은중첩 등 GAS 반환 구조 불문). Node 8케이스(직접·data래핑·json문자열·data.json·이중문자열·value래핑·questions문자열·깊은중첩) 전부 26문항 복원 확인. 못 찾으면 alert+console.warn(원본 구조 출력)으로 진단.
  - ※1차 버전(정해진 키만 보는 방식)은 GAS 실제 반환을 못 풀어 "questions 못 찾음" alert 발생 → 재귀 방식으로 교체해 해결.
  - **시트 상태 확인**: exams 탭에 동지고 2학기 기말 26문항 저장돼 있고 **오늘 수정 최신본**(18번=③,15번=④,3번 table 포함) 반영 확인.
- **백업**: `exam-analysis_BACKUP_20260626_031324.html`(수정 전). ⚠️html 수정이므로 GAS 배포절차 무관하나 백업 보관 필수.
- **남은 검증(필립)**: 실제 브라우저에서 시트 보관목록→클릭→🖨️출력까지 1회 확인. 만약 또 안 되면 콘솔의 '[시험 불러오기] questions 복원 실패' 로그의 원본 항목 구조를 확인(그 형태를 unwrapExamItem에 추가).
- **②증상**: 출력 탭 🖨️ 인쇄 버튼 눌러도 아무 반응 없음(새 창 안 열림). **원인**: printExam(L1487~)이 `window.open('','_blank')` 후 null 체크 없이 `w.document.write` 호출 → **팝업 차단 시 w=null이라 조용히 에러**(alert도 새창도 없음). **수정**: window.open 실패 시 (1)숨김 iframe(`__printFrame`)으로 현재 창에서 인쇄 폴백 (2)팝업 허용 안내 alert. 또 인쇄 가드 메시지를 '등록되지 않았습니다(불러오기/시트클릭 안내)'로 구체화. **필립**: 주소창 팝업차단 아이콘에서 이 사이트 허용하면 새 창으로 정상, 안 해도 iframe으로 인쇄됨.

### [미해결·배포후 과제] 도표(3번) 검토탭 렌더링
- printExam(L1512)은 question을 innerHTML로 → HTML `<table>` 박으면 **인쇄는 격자표 정상**(미리보기 확인).
- 그러나 검토탭 `renderReview`(L998~, 발문 L68 `_rvEsc`)은 question을 **HTML 이스케이프** → 표 태그가 텍스트로 보임(정상 동작이나 도표 수치 검토 불가). 또 검토탭은 question 첫 줄만 발문으로 표시.
- 현재 dongjigo exam.json 3번 question엔 인쇄용 `<table>` 인라인스타일 박아둠(인쇄 OK, 검토탭선 태그 노출).
- **근본 해법(배포후, html 수정)**: exam.json에 구조화 `table` 필드(헤더+행) 신설 → 검토탭·printExam 양쪽이 그 필드를 표로 렌더. 한쪽만 고치면 또 깨지므로 두 곳 동시 수정 + 백업 필수. 배포 임박 시엔 위험하므로 보류.

### [동지고 출제 최종 검증 결과] dongjigo_2026_2f (배포 대상)
- 잡은 오류(수정완료): 해설 정답번호 오타(10·17·19·26), **15번 선지순서 어긋남+정답 ④로 정정**, **18번 무관문장 정답 ⑤→③ 정정**(진짜 무관=건설비 문장), 1·2번 해설 보강. 함정 약한 7·16·17·26 선지 강화(기출 난이도 맞춤).
- 통과: 구조·배점합100·세트지문공유(9-10,12-13)·도표3 수치·삽입(8·20·25 본문논리 확정)·어법어휘 밑줄정합·3종(json/md/review) 정답일치.
- **현재 정답분포 ①6·②4·③6·④6·⑤4**(정답 정확성 우선해 15·18 정정한 결과). 균형 원하면 함의17/빈칸26 선지재배열로 조정 가능(정답 내용 불변 범위).
- **출제허브 호환 남은 2건**: (1)answer가 int → printExam 정답지 기호추출(`^[①②③④⑤]`)과 안 맞음, 문자열화 필요 (2)세트문항 group+passage 미구성 → 10·13번 지문 빈칸. 도표는 위 과제.

### [완료] 이동고3 1학기 기말 예상문제 재출제 (25문항 100점)
- **작업정의**: '재출제'=같은 범위·패턴으로 새 문항 전체 새로. 시트 저장 이동고 패턴 사용.
- **패턴**(시트 exams탭): 어법5·빈칸3·순서3·무관3·주제3·제목2·요약2·어휘2·삽입1·내용1. 정답분포 ①~⑤ 각5. 배점합100.
- **범위**: 수특영어(25·26·28·29·30강)+5모(29·30·32·35·36)+6모(30~41). 지문 풀 전량 확보(수특 passages.js / 모평 pool_sheet.json, 누락0).
- **어법 포인트 매칭**(필립 지시): 기존 기출 포인트 그대로 → 1번 분사 능동/수동, 2번 분사(known), 3번 관계사vs접속사, 5번 조동사+원형. 4번은 기존 'And so on' 관용 지문(29강1)이 기존기출과 겹쳐 25강1로 교체→'요구동사+원형/관계사 수일치'로 대체(안되면 다른포인트 규칙 적용).
- **원유형 회피**: 모평 지문 16개 전부 원유형(ID끝번호) 다른 유형으로 출제, 충돌 0건. 수특 25~30강은 Part2라 회피 불요.
- **검증 통과**: 문항25·배점100·정답분포 각5·유형분포 100%일치·원유형충돌0·정답해설모순0·출처표기25/25. 인쇄 렌더(wkhtmltoimage) 시험지·정답지 육안확인.
- **형식**: 동지고 정립 형식 적용(answer 문자열형, 무관·삽입 선지 번호만+본문번호, 지문 전체, 해설끝 [출처]표기).
- **산출물**: idong_2026_1f_exam.json/md, review.json/html (outputs).
- **7차 검증 완료(오류0)**: 동지고 전체 검증 7회 반복 + 필립 지적사항 적용. 잡은 오류: (1)22·23 어휘 밑줄(<u>) 누락→추가 (2)1번 어법이 어법+어휘 혼재(modifying→programmed)로 불명확→순수 분사 능동/수동(dotting→dotted)으로 재설계, 정답②→④ (3)13번 무관 ④→② 이동(분포 재균형) (4)어법 선지가 번호만→동지고 형식대로 단어표시(① looked 등). 최종: 정답분포 각5·배점100·유형100%·원유형충돌0·정답해설모순0·텍스트깨짐0·출처25/25. 순서9·10·11/삽입24 정답 본문논리 정밀대조 완료.
- **[완료] 출처 강+문항번호 정밀화**: 모평은 이미 번호有(5월35 등). 수특만 'N강'→'N강 M번'으로 갱신(1=28강1번,2=28강2번,3=28강3번,4=25강1번,5=30강1번,14=30강2번,17=25강2번,19=25강3번,21=26강3번). source 필드+해설내[출처:]+md+review+인쇄정답지 모두 반영. ⚠️주의: ID의 _01·_02는 우리 DB 강내 순번이라 실제 수능특강 교재 지문번호와 100% 일치 보장 못함(대체로 맞으나 다르면 필립 조정).
- **잔여(필립)**: 인쇄 시 로컬 파일 업로드 경로 권장. 최종 육안검수 1회 권장(특히 수특 지문번호 교재 대조).

### [완료] 제철고3 1학기 기말 검증·수정 (GitHub 기존본 → 7차 검증)
- **상황**: 제철고3 기말은 이미 GitHub(exams/jcg3f_full_exam.json)에 완성돼 있었음(30문항, 영어II, 중간패턴 적용). '재출제'가 아니라 기존본 검증·수정 작업.
- **패턴**: 빈칸7·어휘5·함축4·순서3·무관3·요약3·삽입2·제목1·요지1·추론1(중간 패턴 동일). 정답분포 ①~⑤ 각6. 배점100.
- **범위**: 수특영독(27STYD 영어독해연습) 1~8강. ※기말범위 지문은 'jechulgo3 기말범위' 시트에 ID목록만 있고 본문은 passages.js에 01강만 있었음. "2027 수능특강 영어독해연습 원본.pdf"(드라이브)에서 05~08강 32지문 추출 가능 확인(EBS_ID=(강-1)*12+번호, 26007-XXXX). 단 기존 기말은 이미 1~8강으로 출제 완료돼 있어 신규 추출 불요였음.
- **27STYD 원유형 규칙**(patterns/styd_original_types.json): 1~4강=빈칸(6·12번=함축), 5~8강=1·4·7·10어법/2·8주제/3·9제목/5·11무관/6·12순서.
- **7차 검증으로 잡은 오류**: (1)**choices=None 10개**(어휘5·무관3·삽입2)→어휘는 밑줄단어 선지, 무관·삽입은 번호만으로 채움 (2)**해설 정답번호 오타 10건**(10·13·14·20·22·23·25·26·28·30번, 대부분 '①이 적절'로 오기됐으나 정답은 다른 번호—해설 내용은 정답과 일치, 번호만 정정) (3)무관 3문항(7·15·18) 전부 정답④지만 내용상 정확·분포균등이라 유지(필립 판단).
- **검증 통과**: 문항30·배점100·정답분포 각6·유형분포 일치·밑줄(어휘5/함축1+)·선지형식·축약0·깨짐0·해설번호0·출처30/30. 순서2·8·11 정답 선지-해설 일치 확인.
- **출처**: source 필드에 강+문항번호+원유형 有('수특영독 3강 5번 (원유형:빈칸추론)'). 해설엔 [출처:]가 없었음→해설끝에 '[출처: 수특영독 N강 M번]' 추가(30/30).
- **산출물**: jechul3_2026_1f_exam.json/md, review.json/html (outputs).
- **잔여(필립)**: 인쇄 로컬 업로드 권장. 무관 3개 모두 ④인 점 변별력 확인. 최종 육안검수 1회.

## 1. 지금 진행 중인 작업 ⬅️ 가장 중요
- **동지고 3학년 기말 예상문제 26문항 — 완료(필립 최종검토 대기). exams/ 폴더 반영만 남음.**
  - **산출 4종**: `dongjigo_2026_2f_exam.md`(시험지+정답해설), `dongjigo_2026_2f_exam.json`(exam-analysis 표준형식 key/meta/questions[26]), `dongjigo_2026_2f_review.json`, `dongjigo_2026_2f_review.html`(와인/골드 검토표).
  - **원문충실도 점검·보정(2026-06-25 추가)**: 출제지문을 원문(passages.js/시트)과 어휘공유율로 정량대조. 보존율 대부분 96~100%, 과변형 없음 확인. 어법은 단어1~3개 오류주입, 어휘는 오답어 1~2개 교체, 무관·삽입은 유형규칙상 문장1개 가감(정상). exam.json 최초판이 지문을 "..."로 축약했던 것 → **전 문항 원문 전체 수록한 완전판 exam.md로 보정 완료**. 15·24번은 시트 원문 인용구 일부가 백슬래시로 잘려 있어 해당부분 제외하고 온전문장만 구성. 삽입형(8·20·25) 정답위치 괄호재배치 후 재검증(④③③ 일치).
  - **⭐원유형 회피 점검(2026-06-25, 기준 정정)**: 원유형 회피 = "지문이 EBS/평가원에서 원래 나온 유형을 피해 다른 유형으로 출제". 모평지문은 ID 끝번호=평가원 문항번호=원유형 고정(29어법/30어휘/31~34빈칸/35무관/36~37순서/38~39삽입/40요약). 동지고가 쓴 모평 4지문(34·32·33 빈칸, 39 삽입) → 전부 다른 유형(제목·어휘·순서·제목)으로 출제, **원유형 회피 4/4 완료**. 수능특강 21~30강은 '주제·소재편'이라 교재상 고정 원유형이 없어 회피 대상 아님. ※(참고: 이동고3 기말 exam.json도 동일 기준으로 모평지문 14개 중 11개 회피한 사례) ※학교 간 지문 중복은 무관 — 회피는 'EBS 원유형' 기준이지 타학교 출제와 비교 아님.
  - **패턴 일치 검증 완료**: 주제2·제목2·도표1·어법3·어휘4·무관2·함의2·삽입3·순서2·내용2·빈칸3 = 26문항 100점. 정답분포 균형(①6/②~⑤각5). 어법 정답정합성 코드검증(4번 seeing→to see, 11번 require→requires, 14번 ⓒ,ⓖ). 15번 '적절한것 1개' 충돌 수정완료(정답④ silent 단일).
  - **동지고 시그니처 재현**: 어법 2종(틀린것1+모두고르기 조합선지), 어휘 4하위유형 전부, 빈칸 3개 전부 '구'4.2점, 영어발문 함의 2개, 한 지문 2문항 세트(9-10 순서+내용일치, 12-13 주제+어휘), 부분키워드·정반대의미어 함정.
  - **다음 할 일**: 필립 OK 시 → exam.json을 레포 `exams/dongji3_2026_2f_exam.json`으로 커밋, review 2종도 동반. (※ 레포 패턴은 신규불요 — `patterns/dongji3_2026_1m_pattern.json` 기존본 사용.)
  - ⚠️ **저작권/한계**: EBS·평가원 지문 변형이라 학원 내부용 한정. 도표(3번)는 풀 내 도표지문 부재로 모평소재 통계표 구성, 안내문 유형(기출19)은 범위 내 부재로 내용일치로 대체 — 실제 동지고가 안내문 낼 가능성 있으면 별도 안내문 지문 확보 필요.
- **일반 상담신청서(apply.html) — GAS 미연결 상태. 배포 후 URL 연결 필요.**
  - apply.html 폼·검증·완료화면 완성. 5개 페이지(main·parent_guide·pohang_highschools·summer·suneung_2026)에 '일반 상담 신청' 버튼 8군데 연결 완료.
  - **남은 일 2가지**: ① consult_gas.gs를 받을 시트(151kfuk4...G18s)의 Apps Script에 붙여넣고 CONFIG.NOTIFY_EMAIL 교체 → 웹앱 배포(모든 사용자 액세스) → URL 복사. ② apply.html L약 320 `const GAS_URL = 'PASTE_...'` 한 줄을 그 URL로 교체. 이 둘 끝나면 작동.
  - ⚠️ consult_gas는 기존 고위험 3종과 **완전 분리된 새 GAS**라 기존 건 안 건드림(안전). 단 배포 절차(새 버전 배포)는 동일 준수.
  - **미정**: NOTIFY_EMAIL(알림 받을 주소) 아직 안 정해짐 → 필립 확인 필요.
- **관리자 페이지 보안 — 2단계(GAS 토큰) 남음. ⚠️ 시험기간이라 보류 중.**
  - 1단계(프론트 공통 게이트) = **완료**(아래 5번). 화면 차단까지 적용됨.
  - 2단계(남은 일): hw-monitor 등의 GAS doGet에 토큰 검사 추가 → "URL 알아도 데이터 못 받게". 1단계는 화면만 가리고 GAS는 여전히 무토큰 응답하므로 이게 진짜 방어선.
  - 구현 방향: auth.js 인증 통과 시 토큰 발급 → 관리 페이지의 모든 jsonp('type=admin...') 호출에 &token= 부착 → GAS doGet 첫 줄에서 토큰 일치 검사, 불일치 시 거부.
  - ⚠️ GAS 작업 철칙(3번) 반드시 준수. **시험 끝난 뒤 별도 세션**에서 진행. 대상 GAS = hw-monitor(우선), 그다음 voca/index 검토.
- ※ 새 작업 시작 시 이 칸에 파일/무엇을/어디까지/다음 할 일을 줄번호·함수명까지 정밀하게 기록

## 2. 보류 / 미적용 (적용 시 주의)
- index.html 대량 학생등록 / 일괄 날짜 기능: 빌드됐으나 미적용. 적용 시 그 시점 최신 index.html에서 처음부터 재구현(이전 버전 재사용 금지)
- **관리자 게이트 2단계(GAS 토큰): 위 1번 참조. 시험기간 종료 후 적용.**

## 3. GAS 상태 ⚠️ 고위험
- 대상 GAS 3종: 단어시험(voca) / 숙제점검(hw-monitor) / index 로그인
- 최근 완성본: Code.gs 759줄 (node --check 통과, route 28개 확인)
- 과거 사고: doGet 없는 부분 스니펫을 붙여넣어 로그인 전체 다운된 적 있음
- 철칙: ①기존 코드 먼저 백업 ②부분 스니펫 절대 금지(전체 파일 교체) ③저장 후 반드시 '새 버전'으로 배포 ④작업 전 어느 GAS인지 확인
- **예정: 보안 2단계에서 hw-monitor GAS doGet에 token 검사 추가 (위 1번)**

## 4. 다음 사이클 예정 작업
- **⭐원유형(原유형) 저장 시스템 — 지문 DB에 orig_type 컬럼 신설 (2026-06-25 분석·시뮬레이션 완료, 구현 대기)**
  - **배경**: 동지고 출제 때 원유형 회피를 Claude가 스스로 못 하고 매번 필립이 지적해야 했음. 근본 원인 = (1) 원유형 데이터가 출제 시점에 없음 (2) 회피가 코드가 아닌 기억 의존. **근본 결함**: 현재 분석 파이프라인이 '정답 반영된 가공지문'을 analyze()에 넣어 분석 → 분석기는 EBS 원본을 본 적 없어 원유형을 알 길이 없음(입구에서 영구 소실). **보완 핵심 = 원유형을 '분석 결과'로 뽑지 말고 '분석 입력'으로 같이 받아 지문 DB에 보존.**
  - **지문 DB 탭 구조(구글시트 "통합분석어시스트" fileId 1_dt7nLh4c9eIRL-05zrGOHFc_m39baVZ7tCelAhRYzw)**: `id | source | chapter | number | difficulty | title | abstract | sentences_json | flow_json | blockSummaries_json | is_public | updated_at | is_hot | chapter_org` (14컬럼). **원유형 컬럼 없음.** 지문 본문·구조가 여기 정규화돼 있음. ※exams 탭(시험 저장, 9컬럼 key|...|json)이 아니라 이 지문 DB 탭이 보완 지점.
  - **확정 설계(시트 최소 변경)**: 이 지문 DB 탭에 **`orig_type` 컬럼 1개만 추가**. 기존 14컬럼·데이터·GAS 전부 무수정. 빈 값=회피 대상 제외(89%가 비어 있어도 기존 시스템 무영향 → 점진 도입 안전).
  - **시뮬레이션 결과(자동 채움 정확도, 안전성 검증됨)**:
    - **모평(MOGO)만 자동·확실**: ID 끝번호=평가원 번호=유형 100%. 이 덤프 구간 17/144건. → `/home/claude/dongjigo/orig_type_mogo_autofill.csv`(id→orig_type 표) 생성해둠, 시트 붙여넣기용. ※전체 시트 모평 빠짐없이 채우려면 전체 모평 ID 재수집 필요.
    - **절대유형(ABSO_TYPE) 자동 불가**: 강=유형이나 강편성표 없음(필립 미보유). number(20~24)는 유형서 내 번호라 원본유형과 다를 여지.
    - **수능특강·영독연습 자동 불가**: number=교재 순번, 유형과 무관.
    - **과거 출제기록 source("절대유형 11강 23번")는 못 씀**: 문항에 붙은 메모지일 뿐 지문 DB id와 연결고리(passage_id) 없음 + 타학교가 같은 지문을 다른 유형으로 변형했을 수 있어 원유형 아님.
    - **결론**: 자동은 모평(약 11%)만. 나머지는 입력 시점 수동(틀린 회피값보다 빈칸이 안전).
  - **teacher.html 수정 지점(passages.js 보호규칙 준수 — 기존 필드 무수정, 신규만 추가)**: L1095 `buildPassageData`에 `orig` 필드 신설 / L2249 지문 붙여넣기 textarea 옆에 원유형 드롭다운 추가(주제/제목/어법/어휘/빈칸/순서/삽입/무관/함의/요약/도표/없음, 모평은 number로 자동선택) / L556 `upsertPassages`·L2189 `buildOutput`에서 orig 필드 누락 안 되게 확인. ※meta.source는 이미 존재(L2653·2849).
  - **권장 구현 순서(안전)**: ①지문DB 탭에 orig_type 빈 컬럼 추가(동작변화 0, 위험 0) → ②모평 autofill CSV 붙여넣기(검증된 값) → ③teacher.html 드롭다운+buildPassageData orig 필드(새 지문 입력 시 1회) → ④출제·검토에서 orig_type 읽어 자동회피(이 단계만 ⚠️GAS, 별도 세션). 모평 number→유형 계산은 시트 수식 말고 **teacher.html 코드에서** 하면 GAS 무관(더 안전).
  - **3단 방어선 최종형**: 1단 데이터(orig_type) → 2단 출제 시 자동출제가 orig 유형 회피 → 3단 저장 전 exam-analysis 검토탭 validatePattern 옆에 '지문×유형이 orig와 겹치면 경고'. 한 겹 새도 다음 겹이 잡음.
  - **작업파일(`/home/claude/dongjigo/`)**: jimun_db_rows.json(지문DB 144행 파싱), jimun_db_sim.json(orig_type 시뮬 결과), orig_type_mogo_autofill.csv.
- **보안 2단계(GAS 토큰) — 시험 종료 직후 1순위.** 데이터의 진짜 빗장(파일 숨겨도 GAS가 무토큰이면 데이터 샘).
- **공개/관리 파일 분리 — 보안 2단계 다음 정리 작업.** 현재 한 저장소·한 폴더에 섞여 hub 하나로 다 노출되는 게 문제의 뿌리.
  - 공개 저장소(현 public): 학생/학부모용만 → index, voca, main, parent_guide, summer, suneung_2026, pohang_highschools
  - 관리용 → 분리 대상: hub, teacher, hw-monitor, exam-analysis, greateng_thumbnail_pro2_2
  - 방식 갈래: (A) **private 저장소로 관리용 이전** = 소스/파일목록 비공개로 가장 강력. 단 무료플랜은 Pages 비공개배포 불가 → 배포 페이지는 URL 알면 열림. 진짜 비공개 배포 원하면 GitHub Pro/Team 등 유료 검토. 무료로 가면 'private 저장소 + auth.js 게이트 + GAS 토큰' 조합이 현실적. (B) **/admin/ 하위폴더로 정리** = 관리·정리·robots/게이트 일괄적용엔 편하나 public인 한 소스 노출 그대로(보안 효과 작음).
  - 권장 순서: ①GAS 토큰 → ②관리 파일 private 분리(배포 비공개 필요 시 유료 검토) → ③auth.js는 보조 게이트로 유지. 결과: '공개=학생용만 / 관리=숨김+토큰+게이트' 3중.
- 보카 DB ↔ 지문 DB 교재명 통일 (현재 불일치: 지문 27ST_E_test2,3 ↔ 보카 '테스트편 1,2,3'; 절대유형 16강 보카 DB 누락; 영독 Mini Test 혼재). 보카 DB를 지문 DB 명명 규칙에 맞추는 방향
- 여름 특강 3개 반(중1-2 / 중3-고1 / 고3 주말 잠정) 커리큘럼·교재 확정

## 5. 최근 완료 (참고용)
- **동지고 3학년 기말 예상문제 26문항 풀세트 → exam.json+review 3종 (2026-06-25)**
  - **출제범위(확정)**: 수능특강 **영어** 21~30강 + **Test2·Test3** + 5·6월 모의고사. ⚠️ID 주의: `27ST_E_`=수능특강 영어(범위 O), `27STYD_`=영어독해연습(범위 X). 수능특강 영어는 Part1(1~20강) 유형고정, **Part2(21~30강)·Test는 유형 고정 없음**(원유형 회피 대상 아님).
  - 동지고 1중간 패턴+선호소재 가중으로 26문항 출제. 산출: dongjigo_2026_2f_exam.md / _exam.json(표준형식) / _review.json / _review.html.
  - **⚠️범위 정정·6문항 전면교체(2026-06-25 후반)**: 초기에 6문항(9·10·12·13·17·23·26)을 영어독해연습(27STYD)으로 잘못 출제 → 전부 수능특강 영어 **Test3** 지문으로 교체. 배정: 9-10세트=test3_002(바이올린 대회 서사, 순서+내용일치) / 12-13세트=test3_005(AI 채용 편향, 주제+어휘a~e) / 17=test3_004(나무 군집 격언, 함축의미) / 23=test3_013(인지부하·창의성, 빈칸) / 26=test3_007(빅데이터 맥락, 빈칸). Test3 12지문 원문은 `/home/claude/dongjigo/test3.json`에 저장(필립이 시트 텍스트로 제공, role·chunk 완비).
  - **Test3 지문 확보 경위**: 구글시트 "통합분석어시스트"의 Test3는 `read_file_content`가 첫 탭(exams)만 반환해 본문 미수신 → 필립이 시트 텍스트 직접 제공으로 해결. (시트 fileId 1_dt7nLh4c9eIRL-05zrGOHFc_m39baVZ7tCelAhRYzw, Test3=27ST_E_test3_001~009·012~014, 010·011 결번)
  - 검증 통과: 정답분포 ①6/②~⑤각5, 유형분포 동지고 패턴 일치, 9번 순서 정답④=(B)-(C)-(A) 정합, 13번 어휘 라벨(a~e) 정합, 어법·15번 단일정답 OK. 동지고 시그니처 전부 재현(어법 2종·어휘 4하위·빈칸3 구·함의 영어발문·2문항세트·함정).
  - **원유형 회피**: 모평 4지문(34·32·33 빈칸, 39 삽입) 전부 다른 유형(제목·어휘·순서·제목)으로 출제 완료. 수능특강 영어 21~30강·Test는 고정 원유형 없어 회피 불요. (참고: 이동고3 exam.json은 같은 기준으로 모평지문 14개 중 11개 회피한 선례 — 단 학교 간 지문 중복은 무관, 회피 기준은 'EBS/평가원 원유형'이지 타학교 출제 비교 아님)
  - **필립 OK 시 exams/dongji3_2026_2f_exam.json 커밋 예정(1번 참조).**
- **일반 상담신청서 apply.html + 상담 전용 GAS (2026-06-24)**
  - 신규 `apply.html`: 네이버 톡톡 없이 신청 가능한 일반 폼. 브랜드(와인/골드/잉크)·Pretendard. 항목=이름·연락처(필수)·연락가능시간(필수)·학교(필수)·학년(필수)·성별·영어실력단계·거주지역(필수)·상담경로·추천인·상담내용(필수,20자↑)·가이드동의(필수)·연락약속동의(필수). 허니팟(name=website) 스팸방지. 제출=fetch POST(form-urlencoded)→완료화면.
  - **진상/쇼핑 거르기**: 가이드정독 필수동의 + 상담내용 20자↑ + 핵심항목 필수 + 상단 안내문. **잠수방지**: 연락처 필수화(톡톡은 선택이었음) + 연락가능시간 필수 + 연락약속 동의 + 즉시 알림메일.
  - 신규 `consult_gas.gs`: 받을시트=151kfuk4...G18s. doPost(시트 기록+서버 재검증+알림메일) + 허니팟 + doGet 헬스체크. 기존 3종과 분리된 새 GAS.
  - 5개 페이지 네이버 버튼 옆에 '일반 상담 신청' 버튼 추가(총 8군데). 기존 네이버 버튼·기존 코드 무변경 확인. apply.html JS 문법 OK.
  - ⚠️ **아직 미작동**: GAS 배포+URL 연결 전(1번 참조). 연결 전까지 일반신청 버튼 누르면 "네이버로 신청" 안내 뜸.
- **parent_guide.html: 시간표 아래 안내문구 편집 기능 추가 (2026-06-24)**
  - 기존 시간표(TUITION) 편집 방식과 동일 패턴으로 `fee-note` 문구도 화면 편집 가능화. 변수 `NOTE_TEXT` 신설(데이터영역) + `<p class="fee-note" id="feeNote">`에 id 부여 + `renderNote()` 함수 추가(보기=textContent, 편집=textarea) + enterEdit/exitEdit에 renderNote 연동 + 코드복사(feeCopyBtn)에 NOTE_TEXT 포함(복사 안내문도 "...NOTE_TEXT까지" 로 갱신).
  - 관리자 비번 `admin4625`(평문, 코드 내 ADMIN_PW). 점(·) 클릭→비번→편집모드. 시간표 기존 기능(반추가/삭제/코드복사) 무변경 확인. JS 3블록 문법 OK.
- **관리자 페이지 보안 1단계 — 공통 프론트 게이트 (2026-06-24)**
  - 신규 파일 auth.js: 보호 페이지 <head>에 <script src="auth.js"></script> 한 줄로 적용. 미인증 시 전체화면 와인/골드 오버레이로 본체 차단 → 비번 통과 시 sessionStorage['greateng_admin_authed']='1' 저장(같은 탭 내 관리 페이지 이동 시 재입력 없음, 탭 닫으면 해제).
  - 비밀번호는 평문 미저장 — SHA-256 해시(PW_HASH)로만 검증. **변경 시 auth.js의 PW_HASH만 교체**(생성법 파일 상단 주석에 명시).
  - 적용된 4개 페이지(각 파일은 auth.js 1줄만 추가, 기존 코드 무수정 / diff 검증 완료):
    hub.html(L4), hw-monitor.html(L4), teacher.html(L5), exam-analysis.html(L4).
  - **게이트 미적용(의도적)**: voca.html·index.html은 이미 자체 인증 보유(voca=voca_admin_authed, index=학생/관리자 비번) → 충돌 방지 위해 제외. 학생용 페이지(index 퀴즈, voca 학생)도 막지 않음.
  - 발견된 구멍(이번 작업 배경): hub.html이 무인증 인덱스라 모든 관리 페이지+GA4 직링크 노출, hw-monitor.html은 인증 0으로 전교생 사진·성적 즉시 표출. 저장소가 public이라 파일명·소스·GA4 ID 공개. → GA4 "관리자 허브" 집계의 외부/본인 구분 이슈가 발단.
  - ⚠️ 한계: 정적 호스팅이라 프론트 게이트는 화면만 가림. GAS는 여전히 무토큰 응답 → 2단계 필수(1번 참조).
  - **유입경로 점검(2026-06-24, 코드만 읽음·무수정)**: 공개 페이지→관리 페이지 '보이는 링크' 유입경로 **없음** 확인. index의 teacher.html 언급=코드 주석/함수명(링크 아님). voca·voca-test의 관리 링크는 모두 `admin-screen`(관리자 로그인 후에만 표시, "학생에겐 안 보임" 주석) 안. 남는 접근은 URL 직접입력뿐 → auth.js 게이트에 걸림. 단, voca/voca-test 소스에 관리 파일명이 평문 노출(화면엔 안 보이나 소스 보기로 확인 가능) → 게이트로 실질 위험 낮고, 공개/관리 분리 시 자연 해소.
- index.html: 로고 축소 완료. 학습 진도 모니터링(시작일/만료일/일일목표/부족분 경고 팝업, 추천공식 30+ceil(부족분×1.5÷(마감-5))), 사각지대 분석, 프리셋 시험범위 선택, 로고 base64→logo.png 분리
- voca.html: 시험결과 3열 레이아웃(명단|결과|단어쓰기), 숙제 14일 그리드, 단어 교재 카테고리 분류, 출제구성 애플 스타일, 전체 단어시험 현황 그리드(캐싱+병렬8), 관리자 한번에 로그인, 허브 메뉴
- hw-monitor.html: 성적 추이 SVG 차트(모의고사/내신 분리), 제출 기록 실시간 로그
- teacher.html: 지문 난이도 저장 우선순위 버그 수정(meta.difficulty 우선)
- 마케팅: main/summer/suneung_2026/parent_guide/pohang_highschools 크로스링크, 파워링크 광고 검토 제출
- 출제: 제철중3·제철고1 예상문제, exam-analysis.html 2단계 검증엔진 + _review 필드
- 행정: 혁신 소상공인 AI활용 지원사업 신청서 제출, 교습비 변경신고(D등급 추가)
