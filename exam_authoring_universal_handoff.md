# 내신 영어 예상문제 출제 — 범용 핸드오프 (학교 무관)

> 이 문서는 **임의의 학교** 기출을 분석해 예상문제를 뽑는 워크플로우다.
> 제철고 고유 데이터(발문·지문·정답)는 포함하지 않는다. 방법론만 담는다.

---

## 0. 시작 전 필수 입력 3종
1. **기출 시험지** (docx/pdf/이미지) — 발문·배점·유형·선지형식 추출용
2. **이번 시험 범위** (교재·강·과 단위)
3. **지문 소스** — 교과서 본문 / 부교재(절대유형 등) / 외부지문(수특영어·영독 등)

---

## 1. 기출 분석 (가장 중요 — 형식 복제의 근거)

기출 docx에서 **반드시 추출**할 것:
- **번호별 유형** (1번=?, 2번=?...) — 패턴의 핵심. 유형 총량이 아니라 **번호별 배치**까지
- **번호별 배점** — 유형·난이도에 따라 다름 (고배점=외부지문 고난도존)
- **발문 텍스트 원문** — 토씨까지 그대로. "~것은?" vs "~고르시오" 구분
- **선지 형식** — 영어/한글, Title Case, 콜론부제, 가운뎃점(···), 하이픈(-) 등
- **세트문항 구조** — [N-M] 안내문, (a)~(e) 표기 등
- **출처 구간 구조** — 보통 앞=교과서(쉬움), 중간=부교재, 뒤=외부지문(어려움)

⚠️ **함정**: "구조 패턴 JSON"이 있어도 `original_q`(발문)가 비어있으면 부정확.
반드시 **기출 실물 docx**에서 발문을 떠와야 한다. 평가원 표준으로 채우면 안 됨(학교마다 다름).

docx 발문 추출 코드:
```python
from docx import Document
import re
d=Document('기출.docx')
for p in d.paragraphs:
    m=re.match(r'^(\d{1,2})\.\s(.+?)\s*\[([\d.]+)점?\]', p.text.strip())
    if m: print(m.group(1), m.group(2), m.group(3))  # 번호 발문 배점
```

---

## 2. 출처 구간 설계 (난이도 곡선의 핵심)

전형적 내신 구조 (학교별 확인 필수):
| 구간 | 출처 | 정답률 목표 | Flesch(가독성) |
|---|---|---|---|
| 앞 (1~4 등) | 교과서 | 75~80% | 높음(50~60, 쉬움) |
| 중간 | 부교재 변형 | 60~70% | 중간(40~47) |
| 뒤 (외부지문존) | 수특영어·영독 등 외부 | 40~50% | 낮음(34 이하, 어려움) |

⚠️ **난이도 역전 주의**: 부교재(절대유형 등) 지문은 서사적이라 쉽다(Flesch~48).
외부지문존에 넣으면 **목표(34)보다 쉬워짐**. 진짜 외부지문(수특 학술)은 Flesch 10~30대.
→ 외부지문존은 **수특영독/영어 최고난도(Flesch 최저) 지문**을 써야 함.

Flesch 측정:
```python
import textstat
textstat.flesch_reading_ease(본문)  # 낮을수록 어려움
```

---

## 3. 원유형 회피 (내신 지문만)

- **교과서·부교재(내신범위)** = 원유형 회피 적용. 각 지문을 EBS 원유형과 **다른 유형**으로 출제
  (예: 원래 제목 지문 → 빈칸/삽입으로, 원래 빈칸 → 어휘/함축으로)
- **외부지문(수특 등)** = 내신범위 아니므로 **회피 불필요**. 학생이 원유형을 공부 안 했음

절대유형 회차구조 참고: 20=주장 21=함축 22=요지 23=주제/제목 24=제목

---

## 4. 원문 최소 손질 원칙 (유형별)

| 유형 | 손질 |
|---|---|
| 어휘 | 밑줄 5개 중 1개만 반의어 교체 (near-synonym 금지, 반의어 원칙) |
| 어법 | 1개만 틀린 형태로 (도치·대동사·분사구문·관계사 등 고난도 포인트) |
| 빈칸 | 1곳만 |
| 순서 | 자르기만 (대명사 참조 간접화로 난이도↑) |
| 무관 | 원문 5문장 + 무관문장 1개 삽입 (주제어 심어 위장) |
| 삽입 | 1문장 빼서 [주어진 문장], 본문 ( ① )~( ⑤ ) 인라인 |
| 제목·요지·함축·요약 | 지문 무변형 |

---

## 5. 난이도 상향 — 선지 설계 (고배점 변별의 핵심)

기출 4점대 문항 분석에서 도출한 원칙:
1. **정답은 가장 밋밋·중립**, 오답은 본문 키워드 보유 + 논조 과장/반전 → 키워드 매칭 무력화
2. **정답을 본문 어휘 0개로 재진술**(추상화), 오답을 본문 전문용어로 도배
3. 오답은 **'상식적으로 맞아 보이는 반대/과장'**
4. 함정 4종: **PARTIAL**(부분만)·**OPPOSITE**(반대)·**OVERGENERALIZATION**(과대)·**BAIT**(무근거 그럴듯)
5. **선지 길이 균등화 필수** — 정답만 길면 위치 단서가 됨 (영어 단어수 ±1~2)
6. 제목 선지: 직설적 명사구/콜론부제 OK, **은유·시적표현 금지** (기출 스타일 확인)

---

## 6. 정답 분포 균등화

- 목표: ①~⑤ 균등 (28문항이면 ⑤·⑥씩)
- **재배치 가능**: 선지 독립 유형 (제목·주제·빈칸·함축·요약·내용일치) — 정답 위치만 이동
- **고정**: 어휘·어법·무관·삽입·순서 — 정답이 본문에 고정돼 못 바꿈
- 재배치 시 **해설의 정답기호도 동기화** ("따라서 ①" → 새 정답으로)

---

## 7. 표준 스키마 v2 (허브 호환)

```json
{
 "key":"학교_학년_년도_학기_시험",
 "meta":{"school","grade","year","semester","exam","subject","subject_code",
         "total","total_score","patterns","answer_dist","score_dist","pattern_key"},
 "questions":[{
   "type":"single","num":N,"qtype":"한글유형","score":N.N,
   "question":"발문\n지문(첫줄=발문, 지문 합침)",
   "choices":["① ...","② ..."],  // 번호기호형(삽입·무관)은 ["①",...]
   "answer":"①","explanation":"해설  [출처: ...]",
   "source":"출처","_orig":"원문","_src":"출처키","_origtype":"원유형"
 }]
}
```

- qtype 한글: 빈칸추론/어휘/어법/함축의미/글의순서/흐름무관/요약문완성/문장삽입/제목/요지/주제/내용일치
- 허브 printExam은 `q.question`만 출력 → **지문을 question에 합쳐야** 빈박스 안 생김
- 함축: 발문+본문에 `<u>밑줄어구</u>` 2회(노출형)
- 요약: 글과 요약문 사이 `⇩`, 선지 `(A) ··· (B)`
- 28번류 어휘세트: 본문 `(a)~(e) <u>단어</u>`, 선지 `["① (a)",...]`

---

## 8. 검증 체크리스트 (출제 후 필수)

- [ ] 유형 총량 = 기출 패턴 일치
- [ ] 번호별 유형·배점 = 기출 일치
- [ ] 발문 = 기출 원문 토씨 일치
- [ ] 총점 = 100 (배점 미세조정 시 상쇄)
- [ ] 정답분포 균등
- [ ] 어법 밑줄 5개 / 어휘 밑줄 5개 / 함축 발문=본문 일치 / 삽입 [주어진문장]+( ① )×5 / 순서 (A)(B)(C) / 요약 ⇩+(A)(B) / 무관 본문번호 5개
- [ ] 선지 5개·빈선지 없음·길이 균등
- [ ] 정답 위치 유효 + 해설 정답기호 동기화
- [ ] 어휘=반의어 교체 확인, 어법=오류 명확
- [ ] 외부지문 Flesch 목표 도달

---

## 9. 작업 규칙 (공통)

- **실행 전 항상 계획 제시 → 확답 후 실행**
- 모든 수정은 최소 diff, 기존 필드 절대 제거 금지 (_orig/_src/_origtype 보존)
- 출력: `/mnt/user-data/outputs/`, 영어 파일명, `present_files`로 공유
- 토큰 효율: targeted grep/sed, raw URL fetch
- 지문DB 시트 CSV: 브라우저 다운로드 + 업로드가 가장 확실 (Drive/GAS 자동접근 자주 막힘)
- @babel 검증은 html 작업 시에만 (JSON은 json.tool로 유효성 확인)

---

## 10. 다음 학교 출제 시작 프롬프트 예시

> "[학교] [학년] [시험] 예상문제 뽑을게.
>  기출 docx 올림. 범위는 [교재 N~M강/과].
>  지문 소스: 교과서 [올림], 부교재 [올림/시트], 외부지문 [수특 시트].
>  이 핸드오프 기준으로 진행해줘."

그러면 Claude가: 기출 분석 → 출처구간 설계 → 유형배치 → 출제(원유형회피·난이도상향) →
정답분포 균등화 → 검증 → exam.json + pattern.json 산출.

---

## 11. 실시간 검증 습관 (★오늘 품질의 핵심 — 자동화 불가, 출제 중 반드시 의심하라)

이 섹션이 "오늘처럼"을 재현하는 진짜 열쇠다. 결과 기준(위 1~10)을 지켜도, **출제 도중 스스로 의심하고 재측정**하지 않으면 품질이 안 나온다. 아래는 매번 능동적으로 수행할 검증이다.

### 11-1. 추측 금지 — 기출 실물로 확인
- "이 학교 함축 선지에 은유가 있나?" "제목이 직설형인가?" → **추측하지 말고 기출 docx를 grep해서 실제 선지를 확인**하고 답하라.
- 발문 한 글자도 추측 금지. "~것은?"인지 "~고르시오"인지 기출에서 확인.
- "이 지문이 외부지문 맞나?" → source 메타·출처구간을 실제로 확인.

### 11-2. 정량 측정 — 느낌 금지
- **선지 길이**: 정답만 길면 위치 단서. 출제 후 `len(c.split())`로 측정해 ±1~2로 균등화.
- **지문 난이도**: "어려워 보인다" 금지. `textstat.flesch_reading_ease()`로 측정.
- **난이도 역전 점검**: 구간별 Flesch를 측정해 앞→뒤로 낮아지는지(어려워지는지) 확인. 부교재 지문을 외부지문존에 넣으면 거꾸로 쉬워지는 함정 발생 → 측정으로만 잡힌다.

### 11-3. 출처-자리 정합성
- 외부지문존에 부교재(절대유형 등) 지문이 들어가 있지 않은지 확인. 외부지문존 = 수특 등 진짜 외부 학술지문(Flesch 최저).
- 교과서존(앞 1~4)에 어려운 지문이 들어가 시작부터 난이도 높아지지 않는지 확인.

### 11-4. 자기모순 점검 (스스로 만든 것 되짚기)
- 정답 재배치 후 → **해설의 "따라서 ①" 정답기호가 새 정답과 맞는지** 재확인. (재배치하면 거의 항상 어긋남)
- 유형 변환(빈칸→함축 등) 후 → 유형 총량이 목표와 맞는지 재계산. 한 곳 바꾸면 총량 깨짐.
- 배점 조정 후 → 총점 100 재확인 (반올림 누적오차 주의).

### 11-5. 사용자 반박을 신호로
- 사용자가 "너무 쉽다" "은유 있나" "이거 맞아?"라고 하면 → **방어하지 말고 즉시 재측정·재확인**. 오늘 품질은 이 캐치볼에서 나왔다.
- 한 번에 다 만들지 말고, 고난도·핵심 문항은 **하나씩 확답받으며** 진행. 빈칸 위치가 핵심주제인지, 선지가 변별되는지 등.

### 11-6. 함정 자가질문 (선지 설계 시 매번)
- 정답이 본문 문장 복붙인가? → 추상화 재진술로.
- 오답이 너무 명백히 틀렸나(상식으로 소거되나)? → 본문 키워드 심어 매력도↑.
- 정답에만 키워드가 붙어 키워드 사냥으로 풀리나? → 정답에서 키워드 빼고 오답에 분산.
- 함정 4종(PARTIAL/OPPOSITE/OVERGENERALIZATION/BAIT)이 골고루 들어갔나?

### 11-7. 측정 도구 빠른 참조
```python
import textstat
textstat.flesch_reading_ease(본문)       # 낮을수록 어려움 (목표: 외부지문 34 이하)
textstat.flesch_kincaid_grade(본문)      # 학년 수준
len(선지.split())                         # 선지 단어수 (균등화용)
# 구간별 평균 Flesch로 난이도 곡선 확인 (앞>중>뒤 순으로 낮아져야 정상)
```

> 핵심: 이 섹션의 검증들은 **출제가 끝난 뒤가 아니라 출제하는 도중에** 능동적으로 돌려야 한다.
> 결과만 보고 "됐다"가 아니라, 매 단계 "정말 그런가?"를 측정·확인하는 것이 오늘의 품질을 만든다.

---

## 12. 출제 강제 체크리스트 (★디테일의 본체 — 출제하면서 매 문항 수행·보고)

이 섹션은 "참고"가 아니라 **강제 절차**다. 출제하는 Claude는 각 문항을 만들 때 아래를 **반드시 수행하고, 그 결과를 사용자에게 보고**해야 한다. 보고를 빠뜨리면 사용자가 "체크리스트 보고는?"으로 잡는다. 이것이 제철고 수준 디테일을 재현하는 유일한 방법이다.

### 12-A. 출제 착수 전 (1회)
- [ ] 기출 docx에서 **번호별 유형·배점·발문 원문** 추출했다 (추측 0)
- [ ] 출처 구간 설계: 앞=교과서 / 중간=부교재 / 뒤=외부지문, 각 Flesch 목표 정함
- [ ] 외부지문은 **Flesch 최저**(가장 어려운) 지문 선별 — 측정해서 골랐다

### 12-B. 매 문항 출제 시 (문항마다)
유형별로 해당 항목을 수행하고, 한 문항 끝낼 때마다 결과를 한 줄로 보고한다.

**모든 유형 공통:**
- [ ] 발문 = 기출 원문 토씨 그대로 (학교 발문체. "고르시오" 등)
- [ ] 정답기호와 해설의 "따라서 ①"이 일치
- [ ] 선지 길이 측정 → 정답만 길지 않게 균등화 (단어수 보고)

**제목·주제·요지:**
- [ ] 정답이 본문 문장 복붙 아님 (본문어휘 0개로 재진술했나)
- [ ] **기출 선지에 은유 있나 확인** → 없으면 직설 명사구로 (제철고는 직설)
- [ ] 오답 4개에 본문 키워드 심어 매력도↑, 함정 4종(PARTIAL/OPPOSITE/OVERGEN/BAIT) 분포

**빈칸:**
- [ ] 빈칸 위치가 **글의 핵심 명제/결론**인가 (문장별 논리 분석으로 확인)
- [ ] 정답이 본문 직접어 아닌 추상 재진술
- [ ] 오답 중 본문에 그대로 있는 것 없나 (복수정답 위험 차단)

**함축:**
- [ ] 밑줄어구가 **비유/함의**를 담나 (직설 어구는 함축 부적합)
- [ ] 발문+본문에 밑줄어구 2회 노출, 동일 텍스트
- [ ] 정답이 진의를 정확히 포착, 표면적 직역 오답 1개 배치

**어법:**
- [ ] 고난도 포인트인가 (도치·대동사·분사구문·관계사 — 병렬·수일치는 너무 쉬움)
- [ ] 밑줄 5개, 틀린 것 1개만, 나머지 4개 어법상 옳음 확인

**어휘:**
- [ ] 밑줄 5개, 반의어 교체 1개 (near-synonym 아닌 명확한 반의어)
- [ ] 교체 위치 = 정답 번호

**순서·삽입:**
- [ ] 연결어·대명사 단서로 **유일 정답** 보장되나
- [ ] (순서) (A)(B)(C) 본문 분할 / (삽입) [주어진 문장] + 본문 ( ① )~( ⑤ )

**무관:**
- [ ] 무관문장에 주제어 심어 위장 (너무 동떨어지면 쉬움)
- [ ] 무관문장은 내가 지어낸 것(원문에 없음), 나머지 5문장 원문 보존

**요약:**
- [ ] 글-요약문 사이 ⇩, (A)···(B) 가운뎃점
- [ ] 본문 어휘 0개로 추상 재기술 (제철고 17번처럼 고급어휘 변별)

### 12-C. 출제 완료 후 (전체)
- [ ] 유형 총량 = 기출 패턴 일치 (재계산)
- [ ] 정답분포 ①~⑤ 균등 (선지독립 유형만 재배치)
- [ ] 총점 = 정확히 100 (반올림 누적 확인)
- [ ] 구간별 Flesch 측정 → 앞>중>뒤 곡선 (역전 없나)
- [ ] 재배치·변환한 문항의 해설 정답기호 재동기화
- [ ] 밑줄·기호 형식 전수 점검 (어법 5개 / (a)~(e) / 함축 노출 / 삽입 번호 5개 / 요약 ⇩)

### 12-D. 보고 형식 (사용자에게)
각 문항 출제 후 한 줄:
```
#21 빈칸 ✓발문기출 ✓핵심명제위치 ✓추상화(복붙0%) ✓복수정답없음 선지10~12단어
```
전체 완료 후 표:
```
유형총량 ✓ | 정답분포 ①6②5③6④5⑤6 | 총점100 | Flesch곡선 44>43>15 ✓ | 형식 ✓
```

> 이 체크리스트를 수행·보고하지 않으면 디테일이 누락된 것이다.
> 사용자는 보고가 없으면 "12번 체크리스트는?"이라고 요구할 수 있다.

---

## 13. 검수 카드(_review) 작성 의무 (★검수자가 빠르게 검수하도록)

출제한 문제를 사용자(전문가)가 **지문 정독 없이 빠르게 검수**하려면, 각 문항에 검수 정보를 미리 써넣어야 한다. exam.json의 각 문항에 `_review` 객체를 **반드시** 포함한다.

### _review 스키마 (문항마다)
```json
"_review": {
  "summary": "지문 요약 2~3줄 — 무슨 내용이고 결론이 뭔지",
  "logic": "글의 핵심 논리/주제 한 줄",
  "answer_basis": "정답이 왜 정답인지 — 본문 어느 부분 근거",
  "distractors": {
    "②": "OPPOSITE — 본문 인과 반대",
    "③": "왜곡 — 보편을 문화고정으로 변질",
    "④": "OPPOSITE — 상호형성을 분리로 뒤집음",
    "⑤": "주객전도 — 상황변이를 유한감정에 종속"
  },
  "difficulty_intent": "정답률 목표와 설계 방식 — 예: 30%대, 본문어휘 0개 재진술로 키워드매칭 차단",
  "review_point": "전문가가 특히 볼 것 — 예: ①과 ④가 둘 다 맥락 언급, 복수정답 아닌지 확인"
}
```

### 작성 규칙
- **summary**: 검수자가 지문을 안 읽어도 내용을 알게. 결론·주제 포함.
- **answer_basis**: "본문 X문장의 Y를 추상화" 식으로 근거 위치 명시.
- **distractors**: 정답 제외한 각 오답에 함정 종류(PARTIAL/OPPOSITE/OVERGEN/BAIT/왜곡) + 한 줄 이유. 어휘/어법/순서/삽입/무관처럼 오답이 선지형이 아닌 유형은 생략 가능하나, 대신 `answer_basis`에 "왜 이게 답이고 나머지는 아닌지" 명시.
- **difficulty_intent**: 12-B에서 정한 난이도 설계를 한 줄로.
- **review_point**: 복수정답 위험, 모호한 선지, 지문-문항 적합성 등 **사람이 꼭 봐야 할 약점**을 정직하게. 없으면 "특이사항 없음".

### 유형별 distractors 작성 예
- **빈칸/제목/주제/함축**: 각 오답 함정 종류 명시 (위 예시처럼)
- **어휘**: `answer_basis`에 "정답=반의어 교체(essential↔irrelevant), 나머지 4개 문맥 적합" 식
- **어법**: `answer_basis`에 "정답=도치 오류(does it→it does), 나머지 4개 어법상 옳음"
- **순서/삽입**: `answer_basis`에 "정답 단서=연결어 Yet/대명사 this, 유일 해 보장"
- **무관**: `answer_basis`에 "무관문장=주제어 있으나 논점(인지과정) 벗어남"

### 핵심
`_review`는 **출제자(나)만 아는 의도**를 검수자에게 전달하는 다리다. 허브 검토탭이 이걸 카드로 표시해, 사용자가 읽고→판단→(필요시)수정한다. 이게 없으면 검수자는 28문항을 전부 정독해야 한다.

---

## 14. 교과서 지문 소스 처리 (DB + 업로드 하이브리드)

교과서 지문은 외부지문(수특)과 달리 **학교·출판사·학년마다 다르다.** 그래서 두 갈래로 관리한다.

### 14-A. 자주 쓰는 교과서 → 지문DB(시트)에 등록
- 우리 지역(포항 남구) 학교들이 쓰는 공통영어1/영어 교과서는 시트 DB에 넣어 재활용.
- **source 명명 규칙**: `TEXTBOOK_{출판사}_{대표저자}` (예: `TEXTBOOK_능률_김성곤`, `TEXTBOOK_YBM_박준언`, `TEXTBOOK_천재_이재영`)
- chapter 필드 = 과(Lesson) 번호. number = 본문 내 지문 순번.
- 등록 시 다른 지문과 동일 스키마(id·sentences_json·difficulty·title 등).
- 한 번 넣으면 같은 교과서 쓰는 모든 학교에 재사용.

### 14-A-1. 검정 교과서 출판사·저자 레퍼런스 (2022 개정)
출제 착수 시 학교가 쓰는 교과서를 이 표에서 찾아 정확한 `source`명을 만든다.

**공통영어1 (2025) / 공통영어2 (2025)** — 동일 라인업:
- NE능률(민병천), NE능률(오선영), YBM(김은형), YBM(박준언), 동아(이병민), 미래엔(김성연), 비상(홍민표), 지학사(신상근), 천재(강상구), 천재(조수경)

**영어Ⅰ (2026) / 영어Ⅱ (2026)** — 동일 라인업:
- NE능률(오선영), YBM(박준언), 동아(박용예), 미래엔(김성연), 비상(홍민표), 지학사(신상근), 천재(강상구), 천재(조수경)

**영어 독해와 작문 (2027):**
- NE능률(민병천), YBM(김은형), 동아(이병민), 미래엔(김대진), 비상(향충배), 천재(윤성호)

**심화영어 (2027):**
- NE능률(안병규), YBM(김명희), 이젠교육(이석재), 천재(신정아)

명명 예시: 공통영어1 YBM 박준언 → `TEXTBOOK_YBM_박준언` / 영어Ⅰ 천재 강상구 → `TEXTBOOK_천재_강상구`
(공통영어1과 공통영어2는 저자가 같으므로 과목까지: `TEXTBOOK_YBM_박준언_공영1` vs `_공영2`)

### 14-B. 드물게 쓰는 교과서 → 출제 시 직접 업로드
- DB에 없는 교과서는 출제할 때 **해당 과 본문을 직접 올린다** (사진/텍스트).
- 범위(예: 3·4과)와 함께 본문을 주면 Claude가 그 자리에서 문항화.
- 자주 나오면 그때 DB로 승격.

### 14-C. 출제 착수 시 교과서 확인 절차
1. 이 학교가 쓰는 교과서 출판사·저자 확인 (사용자에게 물음).
2. 시트 DB에 `TEXTBOOK_{그 출판사}`가 있나 검색.
   - 있으면 → 해당 과 지문 DB에서 로드.
   - 없으면 → 14-B로 본문 업로드 요청.
3. 시험범위의 교과서 과(Lesson)를 정확히 확인 (예: 기말=3~4과).

### 14-D. 주의
- 교과서 지문은 **저작권 민감** — DB는 우리 학원 내부 출제용으로만. 외부 공유 금지.
- 교과서존(보통 1~4번)은 워밍업 = 가장 쉬운 구간. 원유형 회피 적용하되 난이도는 정답률 75~80% 목표.
- 교과서 복붙은 자동검증에서 관대 처리됨(쉬운 구간이라 정상).

> 정리: **자주 쓰는 교과서 = DB(TEXTBOOK_ 접두사) / 드문 것 = 업로드.** 출제 착수 시 교과서부터 확인.
