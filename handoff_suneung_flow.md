# 수능 파이널 · 흐름 독해 커리큘럼 handoff

> 이 문서는 세션 간 연속성을 위한 인수인계 노트입니다. 통째로 새로 쓰지 말고 **변경분만 누적**하세요.
> '진행 중' 항목은 파일·STEP·지문 번호까지 정밀하게 적습니다.
> ⚠️ 기존 `handoff.md`(제철고3 기출복원)와는 **별개 프로젝트**입니다. 서로 덮어쓰지 마세요.

---

## 프로젝트 개요

- **목적**: 고3 4~5등급 → 3등급 목표. "흐름 독해법"을 축으로 한 수능 파이널 프로그램 구축
- **기간**: 2026.7.14(화) ~ 11.19(목) 수능 = **19주**
- **운영**: 화·목반(90분×2) / 토 연강(3h) — **동일 커리큘럼, 시간대만 다름**
- **최종 목표**: 실제 수능 문제 풀 때 도움 되는 능력 — 특히 ①문단 초반 핵심어+속성 파악 ②복잡·이중적 표현을 학생 언어로 단순화

---

## 최종 산출물 (outputs/)

| 파일 | 용도 | 상태 |
|---|---|---|
| `suneung_curriculum_2027.html` | 19주 커리큘럼 로드맵. 3블록(흐름8주/이항대립4주/실전7주), 주차별 화·목·토 분배, 빈칸 점증 수준 명시, 9월 모평 분기점 | **완료** |
| `flow_workbook_block1.html` | 블록1 흐름 독해 워크북. 기출 9지문 원문(문장번호 ①②③ 포함) + STEP 1~5. 학생/교사 × 초/중/고급 토글, 인쇄 | **완료** (STEP1 강화·STEP2 신설 반영본) |
| `binary_workbook_block2.html` | 블록2 이항대립 워크북. 기출 7지문 + O/△ 좌우 분류표 + STEP 1~4. 초/중/고급 차등(100%채움→첫줄만예시→백지) | **완료** (중·고급 차등 재설계 반영본) |
| `passage_analysis_db.html` | 기출 16지문 분석 DB(흐름9+이항7). 출처·유형·흐름구조·O축/△축·난이도·용도 | **완료** (⚠️ 필립 검수 대기) |
| `flow_workbook_suneung5.html` | **구버전** 5지문 워크북(설득·걱정·나쁜사과·토론필기·수도원). 영문 원문 비어있음. block1으로 대체됨 | 폐기 예정 |
| `flow_workbook_v3.html` / `v2` / `sample` | 초기 설계 반복본(자작 지문). 디자인 레퍼런스로만 | 참고용 |

---

## 커리큘럼 구조 (19주)

| 블록 | 주차 | 내용 |
|---|---|---|
| **BLOCK 1** | W1–8 | 흐름 독해법 체화. **W1에 5단계 전체를 한 번에** 가르치고, W2~7은 같은 방법을 **빈칸 늘려가며** 반복 |
| **BLOCK 2** | W9–12 | 이항대립 7축 × 문제유형(빈칸/순서/삽입/무관) |
| **BLOCK 3** | W13–19 | 실전. 약점 보강, 시간 운영, 오답 트리 복기 |

### 빈칸 점증 (블록1 핵심 설계)
W1 번호만 → W2 번호+라벨 → W3 라벨+전환점 → W4 머리만제공(딸림 직접) → W5 전환·요지까지 → W6 백지100% → W7 백지+시간

### 시험 주차
- **9/2(수) 9월 모평 = 평가원 = 진단 분기점** (W8). 화 최종점검 → 수 시험 → 목 오답 트리 복기 → 토 약점 클리닉 → 블록2 방향 확정
- 10월 교육청 = 출제주체 다름, **참고용 1회로만 가볍게** (W14, 진도 유지)

---

## 워크북 STEP 구조

### 블록1 (흐름 독해) — STEP 1~5
1. **첫 문장 해부** ← *필립 요청 반영, 강화됨*
   - ①소재(핵심어) ②속성(어떻게 보나) ③**속성 방향**(긍정/부정/통념·양보)
   - "통념·양보" 선택 시 → "뒤에 However·But·Rather로 뒤집힌다" 힌트 표시
2. **내 말로 바꾸기** ← *필립 요청 반영, 신설*
   - 지문당 어려운 표현 2~3개 → 직역 아닌 쉬운 자기 말로 한 줄 변환
   - 교사용에 모범 단순화 짝지어 표시
3. **흐름 트리 작도** (머리/딸림 8라벨, 전환선으로 줄기 분리)
4. **흐름 전환점** (신호어 + 위치)
5. **요지 재구성** (두 줄기 머리 문장만 이어서)

### 블록2 (이항대립) — STEP 1~4
1. 두 축 찾기 (O / △)
2. O/△ 분류표 (좌우 대조)
3. 축 전환점
4. 문제 적용 (해당 유형 풀이)

---

## 확정 지문 목록

### 블록1 흐름 독해 9지문 (난이도 계단식)
| # | 출처 | 유형 | 흐름구조 | 난이도 |
|---|---|---|---|---|
| 1 | 2025수능 20 | 주장 | 통념(게임=방해) → While/Rather 반전 | ★ |
| 2 | 2024수능 20 | 주장 | 가치만으론 부족(양보) → 행동 공유 지침 | ★ |
| 3 | 2025-9월 20 | 주장 | 진실 중요 → However 위협 → 사용자 책임 | ★ |
| 4 | 2025-6월 20 | 주장 | AI로 역할축소(통념) → However 인간이 감독 | ★ |
| 5 | 2024-3월 20 | 주장 | sage(통념) → Rather than → guide | ★ |
| 6 | 2025-9월 24 | 제목 | 직선발전(통념) → but myth → 순환반복 | ★★ |
| 7 | 2025수능 23 | 주제 | 산업화 → 시간·노동 재편 (인과) | ★★ |
| 8 | 2025-6월 23 | 주제 | 자연 표방(겉) ↔ 의미 분리(실제) | ★★ |
| 9 | 2025-6월 21 | 함의추론 | 흑백론 → cannot account → 스펙트럼 | ★★ |

### 블록2 이항대립 7지문 (7축)
| # | 출처 | 축 | O | △ | 유형 | 주차 |
|---|---|---|---|---|---|---|
| 1 | 2025-6월 34 | 본성vs양육 | 자기충족(개인 내재) | 사회적 형성 | 빈칸 | W9 |
| 2 | 2024수능 33 | 이성vs경험 | 얼굴 단독 정보 | 맥락·상황 | 빈칸 | W9 |
| 3 | 2024-3월 36 | 관습vs혁신 | 즉흥(재즈) | 작곡·분석(모차르트) | 순서 | W10 |
| 4 | 2025-9월 39 | 예측vs불확실 | predictable | unprecedented | 삽입 | W11 |
| 5 | 2024-3월 41~42 | 객관vs주관 | 객관적 사건 | 주관적 narration | 장문 | W12 |
| 6 | 2024-3월 23 | 개인vs공동체 | 개인으로 추론 | 부족(tribe) 일원 | 주제 | W12 |
| 7 | 2024수능 23 | 시장vs정부 | 시장가치(벌목) | 비시장가치(보존) | 주제 | W12 |

> **제외**: 변증법(정-반-합)은 3축이라 이항 훈련에 부적합 + 보유 5회분에 마땅한 지문 없음. 2등급 목표 시 별도.
> **보너스(여유 시)**: 결정론vs확률, 아날로그vs디지털, 내재vs외재 동기(2025-6월 30)

---

## 저작권 처리 방침 (중요)

- 평가원·교육청 기출 원문은 **필립이 정식 보유·업로드한 자료**를 본인 학원 수업용으로 사용하는 것 → 워크북에 원문 직접 삽입함
- 문장 번호(①②③)는 Claude가 자동 부여
- 초기 버전(`flow_workbook_suneung5.html`)은 원문 비워두고 붙여넣기 방식이었으나, **현재는 원문 포함 방식으로 전환**
- 블로그·홍보물 등 **외부 공개물에는 원문 미노출** 원칙 유지(주제 메모로 대체) — 기존 제철고 프로젝트 방침과 동일

---

## 진행 중 / 미완 항목

**0-A. ⏳ 블록2 워크북에 STEP 강화 미적용**
- 블록1에 넣은 **STEP1(첫 문장 해부·속성 방향)** 과 **STEP2(내 말로 바꾸기)** 를 블록2에는 아직 안 넣음
- 필립에게 "블록2에도 넣을까요?" 질문한 상태에서 세션 종료
- 넣을 경우: `/home/claude/work/wb_b2.json`의 각 지문에 `key`/`attr`/`dir`/`simplify` 필드 추가 → `gen_b2.py`의 STEP 구조에 삽입 → STEP 번호 1~6으로 재정렬
- 블록1 작업 방식 그대로 재현 가능 (`add_drill.py` 참고)

**0-B. ⏳ 분석 DB 검수 → Google Sheets 이관**
- `passage_analysis_db.html` 16지문은 **Claude의 1차 분류**. 필립 검수 필요
- 특히 확인 요망: ①이항대립 O/△ 배정 ②난이도 등급 ③b6(개인vs공동체) — O축이 "글이 부정하는 쪽"이라 분류가 까다로움
- 검수 후 2단계: Google Sheets(통합분석어시스트) 이관 + GAS 백엔드 연동 → 축·유형·난이도 필터링, 회차 누적
- ⚠️ GAS 작업 시 규칙 준수: 기존 코드 전체 백업 → 일부만 붙여넣지 말 것 → 저장 후 새 버전 배포 → 어느 GAS인지 먼저 확인

**0-C. ⏸ 지문 자동분석 도구 (보류)**
- "지문 넣으면 자동으로 트리/축 생성" — teacher.html 계열 도구로 구상했으나 **보류**
- 정확도 논의 결과: 문장분리 ~100% / 신호어 ~95% / 머리·딸림 라벨링 **85~90%**(Gemini Flash) → 교사 검수 필수
- 설계 방향(합의됨): AI 초안 생성 + **교사 검수·수정 모드**(머리↔딸림 토글, 라벨 변경, 가지 이동) + 모델 선택지(Flash/Pro/Claude)
- 재개 시: 이번에 정립한 7축 정의·흐름구조 분류 기준을 **분석 프롬프트의 기준**으로 이식할 것 (자산 재활용 3단계 계획의 3단계)
- API 키는 코드에 하드코딩 금지, 필립이 직접 입력하는 자리로

**0-D. ⏳ 블록3(실전 7주) 자료 미착수**
- W13~19 실전 블록은 커리큘럼 표만 있고 실제 자료 없음
- 필요 시: 오답 트리 복기 양식, 시간 배분 가이드, 약점 유형 클리닉지

**0-E. ⚠️ Gemini 매칭 리스트 검증 필요**
- 필립이 이전에 Gemini와 만든 "10회차 이항대립 기출 매칭 리스트"의 **영어 지문 출처 번호는 미검증**
- 이번 세션에서 Claude가 보유 5회분으로 재매칭했으므로, 그 리스트의 번호는 **참고만** 할 것
- 예: "2022수능 34번 유전적 본능과 환경적 적응" 등은 원문 대조 안 됨

---

## 작업 스크립트 위치 (/home/claude/work/)

| 파일 | 역할 |
|---|---|
| `wb9.json` | 블록1 9지문 데이터(원문·트리·소재/속성/방향·simplify) |
| `wb9_data.py` | 위 JSON 생성 |
| `add_drill.py` | wb9.json에 key/attr/dir/simplify 추가 (STEP1·2 강화용) |
| `gen_wb9.py` | 블록1 카드 HTML 생성 |
| `assemble_wb9.py` | 블록1 최종 조립(템플릿+CSS) |
| `wb_b2.json` | 블록2 7지문 데이터(원문·O/△·pivot·apply) |
| `wb_b2_data.py` | 위 JSON 생성 |
| `gen_b2.py` | 블록2 카드 HTML 생성 (초/중/고급 차등 포함) |
| `assemble_b2.py` | 블록2 최종 조립 |
| `analysis.json` / `build_analysis.py` / `gen_analysis_html.py` | 분석 DB |

> ⚠️ 컨테이너는 세션마다 초기화됨. 다음 세션에서 수정하려면 outputs의 HTML을 직접 편집하거나 스크립트를 재생성해야 함.

---

## 디자인 토큰 (브랜드 일관)

- 와인 `#7a1020` / 와인soft `#9a2435` / 골드 `#C9A84C` / 골드soft `#e3cf95` / 잉크 `#3a2a20` / 페이퍼 `#fbf8f2` / 라인 `#e0d6c0`
- 블록2 구분색(초록) `#3b6d5a`
- 폰트: Noto Sans KR(본문) + Space Grotesk(영문·번호·라벨)
- 난이도 색: ★ `#3b8a5a`(초록) / ★★ `#c8941a`(노랑) / ★★★ `#b3402a`(빨강)

---

## GitHub 배포 (예정)

레포: `greatenglish1201-ux/GREATENG` (GitHub Pages)

```
https://greatenglish1201-ux.github.io/GREATENG/flow_workbook_block1.html
https://greatenglish1201-ux.github.io/GREATENG/binary_workbook_block2.html
https://greatenglish1201-ux.github.io/GREATENG/suneung_curriculum_2027.html
https://greatenglish1201-ux.github.io/GREATENG/passage_analysis_db.html
```

> 아직 커밋 안 됨. 필립 수동 업로드 필요.

---

## 다음 세션 시작 시 체크리스트

1. 이 문서 + `passage_analysis_db.html` 먼저 확인
2. 블록2에 STEP 강화 넣을지 결정 (0-A)
3. 분석 DB 검수 결과 있으면 반영 후 Sheets 이관 (0-B)
4. 필요 시 블록3 실전 자료 착수 (0-D)
